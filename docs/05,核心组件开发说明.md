# 核心组件开发说明

> 记录第 5 步创建核心组件的过程，便于学习和参考

---

## 一、什么是核心组件？

### 核心组件的定义

核心组件是构成页面主要内容的组件，是页面的"积木"。

```
┌─────────────────────────────────────────┐
│              Home 页面                    │
│  ┌─────────┐  ┌─────────────────────┐    │
│  │  Hero   │  │  CategorySection    │    │
│  └─────────┘  └─────────────────────┘    │
│  ┌─────────────────────────────────────┐ │
│  │  CategorySection × 4                │ │
│  └─────────────────────────────────────┘ │
│  ┌─────────────────────────────────────┐ │
│  │  GridCard × 4                       │ │
│  └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### 核心组件 vs 基础组件

| 类型 | 说明 | 示例 |
|------|------|------|
| **基础组件** | 简单的 UI 元素 | Button、Footer |
| **核心组件** | 页面主要内容 | Hero、CategoryCard |

---

## 二、组件设计原则

### 2.1 Props（属性）设计

组件通过 Props 接收数据和配置：

```tsx
// CategorySection 组件
interface CategorySectionProps {
  category: Category;  // 必填数据
}

export function CategorySection({ category }: CategorySectionProps) {
  return (
    <div>
      <img src={category.coverImage} alt={category.name} />
      <h2>{category.name}</h2>
    </div>
  );
}

// 使用组件
<CategorySection category={categories[0]} />
```

### 2.2 类型安全的 Props

```typescript
// 定义类型
interface ProjectCardProps {
  project: Project;          // 必填
  onClick?: () => void;      // 可选
  className?: string;        // 可选（扩展用）
}

// 使用默认值
export function ProjectCard({
  project,
  onClick,
  className = '',
}: ProjectCardProps) {
  // ...
}
```

### 2.3 组件的职责

每个组件只做一件事：

```
✅ Hero：只负责首页首屏的显示
✅ CategorySection：只负责分类区块的显示
✅ ProjectCard：只负责项目卡片的显示

❌ 错误：Hero 负责首屏 + 分类区块（职责过多）
```

---

## 三、核心组件详解

### 3.1 Hero 组件

```tsx
// src/components/Hero.tsx
import { ArrowRight } from 'lucide-react';
import { personal } from '../data';
import { Button } from './Button';

interface HeroProps {
  onContactClick?: () => void;  // 可选回调
}

export function Hero({ onContactClick }: HeroProps) {
  return (
    <section className="h-[45vh] min-h-[350px] flex flex-col items-center justify-center text-center px-6 bg-white">
      {/* 头像 */}
      <img
        src={personal.avatar}
        alt={personal.name}
        className="w-24 h-24 rounded-full object-cover mb-6"
      />
      {/* 标题 */}
      <h1 className="text-3xl md:text-4xl font-bold text-[#1D1D1F] mb-3">
        我是 {personal.name}
      </h1>
      {/* Slogan */}
      <p className="text-lg text-[#86868b] mb-6 max-w-2xl">
        {personal.slogan}
      </p>
      {/* CTA 按钮 */}
      <div className="flex gap-4">
        <Button variant="primary">了解更多</Button>
        <Button variant="secondary" onClick={onContactClick}>
          联系方式 <ArrowRight className="inline ml-2" size={16} />
        </Button>
      </div>
    </section>
  );
}
```

**Props：**
| 属性 | 类型 | 说明 |
|------|------|------|
| onContactClick | () => void | 点击联系方式按钮的回调 |

### 3.2 CategorySection 组件

```tsx
// src/components/CategorySection.tsx
import { Link } from 'react-router-dom';
import { ArrowRight } from 'lucide-react';
import type { Category } from '../data/types';
import { Button } from './Button';

interface CategorySectionProps {
  category: Category;
}

export function CategorySection({ category }: CategorySectionProps) {
  return (
    <section className="w-full py-12 px-6 border-t border-[#F5F5F7] bg-white">
      <div className="max-w-6xl mx-auto">
        <div className="flex flex-col lg:flex-row gap-8 items-center">
          {/* 图片区域 - 40% */}
          <div className="w-full lg:w-2/5">
            <img
              src={category.coverImage}
              alt={category.name}
              className="w-full h-[250px] lg:h-[300px] object-cover rounded-2xl shadow-sm"
            />
          </div>

          {/* 内容区域 - 60% */}
          <div className="w-full lg:w-3/5 text-center lg:text-left">
            <h2 className="text-2xl md:text-3xl font-bold text-[#1D1D1F] mb-3">
              {category.name}
            </h2>
            {category.description && (
              <p className="text-base text-[#86868b] mb-6 leading-relaxed">
                {category.description}
              </p>
            )}
            <Link to={`/category/${category.id}`}>
              <Button variant="secondary">
                查看更多 <ArrowRight className="inline ml-2" size={16} />
              </Button>
            </Link>
          </div>
        </div>
      </div>
    </section>
  );
}
```

**Props：**
| 属性 | 类型 | 说明 |
|------|------|------|
| category | Category | 分类数据对象 |

### 3.3 ProjectCard 组件

```tsx
// src/components/ProjectCard.tsx
import type { Project } from '../data/types';

interface ProjectCardProps {
  project: Project;
}

export function ProjectCard({ project }: ProjectCardProps) {
  return (
    <div className="bg-white rounded-xl overflow-hidden shadow-sm hover:shadow-lg transition-shadow duration-200">
      {/* 项目图片 */}
      <img
        src={project.image}
        alt={project.title}
        className="w-full h-48 object-cover"
      />
      {/* 项目内容 */}
      <div className="p-6">
        <h3 className="text-lg font-semibold text-[#1D1D1F] mb-2">
          {project.title}
        </h3>
        <p className="text-[#86868b] mb-4">{project.description}</p>

        {/* 技术栈标签 */}
        <div className="flex flex-wrap gap-2 mb-4">
          {project.techStack.map((tech) => (
            <span
              key={tech}
              className="px-3 py-1 bg-[#F5F5F7] text-[#86868b] text-sm rounded-full"
            >
              {tech}
            </span>
          ))}
        </div>

        {/* 项目链接 */}
        <a
          href={project.link}
          target="_blank"
          rel="noopener noreferrer"
          className="text-[#007AFF] hover:underline text-sm"
        >
          查看详情 →
        </a>
      </div>
    </div>
  );
}
```

**Props：**
| 属性 | 类型 | 说明 |
|------|------|------|
| project | Project | 项目数据对象 |

### 3.4 GridCard 组件

```tsx
// src/components/GridCard.tsx
import { Link } from 'react-router-dom';
import type { GridItem } from '../data/types';

interface GridCardProps {
  item: GridItem;
}

export function GridCard({ item }: GridCardProps) {
  return (
    <Link
      to={item.link}
      className="block bg-white rounded-xl p-6 text-center hover:shadow-lg transition-all duration-200 hover:-translate-y-1"
    >
      <h3 className="text-base font-semibold text-[#1D1D1F] mb-2">
        {item.title}
      </h3>
      {item.description && (
        <p className="text-xs text-[#86868b]">{item.description}</p>
      )}
    </Link>
  );
}
```

**Props：**
| 属性 | 类型 | 说明 |
|------|------|------|
| item | GridItem | 网格项数据对象 |

---

## 四、组件组合使用

### 4.1 Home 页面的组件组合

```tsx
// src/pages/Home.tsx
import { useNavigate } from 'react-router-dom';
import { categories, gridItems } from '../data';
import { Hero } from '../components/Hero';
import { CategorySection } from '../components/CategorySection';
import { GridCard } from '../components/GridCard';
import { Footer } from '../components/Footer';

export function Home() {
  const navigate = useNavigate();

  // 处理联系方式点击
  const handleContactClick = () => {
    navigate('/contact');
  };

  return (
    <div className="min-h-screen bg-white">
      {/* Hero 组件 */}
      <Hero onContactClick={handleContactClick} />

      {/* 分类区块 */}
      {categories.map((category) => (
        <CategorySection key={category.id} category={category} />
      ))}

      {/* 底部网格 */}
      <section className="py-16 px-6 bg-[#F5F5F7]">
        <div className="max-w-6xl mx-auto">
          <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
            {gridItems.map((item) => (
              <GridCard key={item.id} item={item} />
            ))}
          </div>
        </div>
      </section>

      <Footer />
    </div>
  );
}
```

### 4.2 CategoryDetail 页面的组件组合

```tsx
// src/pages/CategoryDetail.tsx
import { useParams, Link } from 'react-router-dom';
import { ArrowLeft } from 'lucide-react';
import { categories, projects } from '../data';
import { ProjectCard } from '../components/ProjectCard';
import { Footer } from '../components/Footer';

export function CategoryDetail() {
  const { id } = useParams<{ id: string }>();
  const category = categories.find((c) => c.id === id);
  const categoryProjects = projects.filter((p) => p.categoryId === id);

  if (!category) {
    return <div>分类不存在</div>;
  }

  return (
    <div className="min-h-screen bg-white">
      <Link to="/">← 返回首页</Link>

      <h1>{category.name}</h1>
      <p>{category.description}</p>

      {/* 使用 ProjectCard 组件 */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {categoryProjects.map((project) => (
          <ProjectCard key={project.id} project={project} />
        ))}
      </div>

      <Footer />
    </div>
  );
}
```

---

## 五、重构效果

### 5.1 重构前后的代码行数

| 文件 | 重构前 | 重构后 | 减少 |
|------|--------|--------|------|
| Home.tsx | 73 行 | 42 行 | -42% |
| CategoryDetail.tsx | 76 行 | 77 行 | 差不多 |

### 5.2 重构的好处

| 好处 | 说明 |
|------|------|
| **代码复用** | CategorySection 可以在任何地方使用 |
| **易于维护** | 修改一个组件影响所有使用它的地方 |
| **可测试性** | 可以单独测试每个组件 |
| **可读性** | 页面代码更简洁，逻辑更清晰 |

---

## 六、常见问题

### Q: 什么时候创建新组件？
**A:** 当一个代码块重复使用 2 次以上，或者代码量超过 50 行时，考虑提取为组件。

### Q: 组件命名规范？
**A:** 使用 PascalCase（大驼峰），如 `ProjectCard`、`CategorySection`。

### Q: Props 太多怎么办？
**A:** 如果 Props 超过 5 个，考虑将相关 Props 合并为一个对象，或者拆分成多个组件。

### Q: 组件之间如何通信？
**A:**
- 父组件 → 子组件：通过 Props 传递
- 子组件 → 父组件：通过回调函数
- 兄弟组件：通过父组件中转

### Q: 什么是回调函数？
**A:** 父组件传给子组件的函数，子组件在特定时机调用：

```tsx
// 父组件
function Home() {
  const handleClick = () => {
    navigate('/contact');
  };

  return <Hero onContactClick={handleClick} />;
}

// 子组件
function Hero({ onContactClick }: HeroProps) {
  return <Button onClick={onContactClick}>联系方式</Button>;
}
```

---

## 七、扩展阅读

| 主题 | 链接 |
|------|------|
| React 组件 | https://react.dev/learn/your-first-component |
| Props 传递 | https://react.dev/learn/passing-props-to-a-component |
| 组件设计 | https://react.dev/learn/thinking-in-react |

---

## 八、下一步

第 6 步将完善页面，添加动画效果和响应式优化。

---

> 最后更新日期：2026-01-25
